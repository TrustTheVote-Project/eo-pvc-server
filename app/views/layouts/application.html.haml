!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'Eo Pvc Server'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'Eo Pvc Server'}"}
    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
  %body
    %header
      = render 'layouts/navigation'
    %main{:role => "main"}
      = render 'layouts/messages'
      = yield
      - if current_user
        %script{type: "text/javascript"}
          checkInAppNotifications("#{current_user.id}")
          
    #demo_id
      = "Demo ID: #{current_user.demo_id}"
    %footer
      .container
        %nav.navbar
          = link_to "Contact", "#"
          = link_to "Terms Of Use", "#"
          = link_to "Privacy Statement", "#"
        %p
          &copy; 2019 Elections Ontario. All Rights Reserved.