= notifications_header
- if @notifications.empty?
  %h3= t('notification.none')
- else 
  .notifications
    - @notifications.each do |n|
      .notification
        %h3{class: n.read ? 'read' : 'unread'}
          = link_to notification_path(n) do
            %strong= n.title