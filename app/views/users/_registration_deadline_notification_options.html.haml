= f.select :registration_deadline_notification_options, options_for_select((0..21).to_a, f.object.registration_deadline_notification_options || 14)

= f.label :registration_deadline_notification_options, t('notifcation.days_before_deadline')